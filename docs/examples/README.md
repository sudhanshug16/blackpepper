# Examples

Store short examples for CLI flows and command-mode usage here.

Suggested files:

- `command-mode.md` for `:create`, `:destroy`, and PR workflows.
- `workspace-setup.md` for `git worktree` flows.
